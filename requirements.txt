# venv, cpu only.
#   pip install --no-cache-dir -r requirements.venv.txt

--extra-index-url https://download.pytorch.org/whl/cpu

torch
torchvision

ultralytics>=8.3.0
matplotlib>=3.9.0
pyyaml>=6.0

# Dataset versioning. The [s3] extra pulls in dvc-s3, which is what backs the
# s3:// remote in .dvc/config; plain `dvc` cannot talk to S3 without it.
dvc[s3]>=3.0
kubeflow
boto3