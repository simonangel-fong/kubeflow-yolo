apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-storage-classes
  namespace: argocd
  labels:
    scope: platform
    capability: storage
  annotations:
    argocd.argoproj.io/sync-wave: "20"
  finalizers:
    - argocd.argoproj.io/resources-finalizer
spec:
  project: default
  source:
    repoURL: https://github.com/simonangel-fong/kubeflow-yolo
    targetRevision: master
    path: argocd/storage/storage-classes
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # StorageClass.parameters is immutable, so a patch fails once the class
      # exists. Recreate instead. Bound PVCs and their data are unaffected:
      # the class is only read when a volume is provisioned.
      - Replace=true
