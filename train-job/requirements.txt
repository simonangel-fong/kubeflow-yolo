# GPU only (CUDA 13.0), matching the torch build in the Kubeflow notebook image
# running on g5.xlarge (A10G).
#   pip install --no-cache-dir -r train-job/requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu130

torch
torchvision

ultralytics>=8.3.0
matplotlib>=3.9.0
pyyaml>=6.0

# Tracking. psutil and pynvml back mlflow's system-metrics sampler; without
# pynvml the GPU series are silently absent. boto3 is what mlflow uses to push
# artifacts to the s3:// artifact store.
mlflow>=3.0
psutil
pynvml
boto3
