# GPU (CUDA 13.0), matching the torch build in the Kubeflow notebook image
# running on g5.xlarge (A10G).
#   pip install --no-cache-dir -r train-job/requirements.gpu.txt

--extra-index-url https://download.pytorch.org/whl/cu130

torch
torchvision

ultralytics>=8.3.0
matplotlib>=3.9.0
pyyaml>=6.0
