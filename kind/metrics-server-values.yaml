#   helm install metrics-server metrics-server/metrics-server \
#     --namespace kube-system -f kind/metrics-server-values.yaml
args:
  - --kubelet-insecure-tls

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    memory: 128Mi
